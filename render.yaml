services:
  # Backend Service
  - type: web
    name: navora-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    region: oregon
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8080
      - key: MODE
        value: simulation

  # Frontend Service
  - type: web
    name: navora-frontend
    env: node
    buildCommand: cd frontend && npm install && npm run build
    startCommand: npx http-server frontend/dist -p 10000 -c-1 --cors
    plan: free
    region: oregon
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: navora-backend
          property: url